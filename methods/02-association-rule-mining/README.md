# 02 · Surfacing Salient Attributes: Association Rule Mining

Find the attribute bundles tied to each status position (`arules`): which attributes signal on their own, which only in bundles, and where the same attribute shifts meaning across pairings (polysemy).

- **Run on your own data:** `mine-attribute-bundles.R` (calls functions in the repo-root `R_functions/`).
