# output/

The scripts in `methods/` save their figures and tables here. The files
are not tracked by git; run the scripts to create them. Files produced
from the bundled synthetic data start with `synthetic_`; files from your
own data use plain names.
